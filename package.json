{
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:open:chrome": "npx cypress open --browser chrome",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cypress:open:dev": "cypress open --config-file config-dev.js --browser chrome ",
    "cypress:open:qa": "cypress open --config-file config-qa.js --browser chrome "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^15.10.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "cypress-multi-reporters": "^2.0.5",
    "dotenv": "^17.3.1",
    "mocha-junit-reporter": "^2.2.1"
  }
}
